version: '3.3'

services:
  juiceshop:
    image: "bkimminich/juice-shop"
    ports: 
      - "3000:3000"
    healthcheck: 
      test:  ["CMD", "curl", "-f", "-start-period=30","http://localhost:3000"]

