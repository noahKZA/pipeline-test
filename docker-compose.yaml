version: '3.3'

services:
  juiceshop:
    image: "bkimminich/juice-shop"
    ports: 
      - "3000:3000"
    healthcheck: 
      test:  ["CMD", "wget", "http://localhost:3000"]
      interval: 10s
      start-period: 20s

